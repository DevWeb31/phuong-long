/**
 * Page d'accueil du dashboard (vide pour l'instant)
 * Permet de différencier clairement l'URL /dashboard/home de /dashboard/profile.
 */

export default function DashboardHomePage() {
  return (
    <div
      className="min-h-[300px] rounded-xl border border-dashed border-white/10"
      aria-label="Section tableau de bord vide"
    />
  );
}

